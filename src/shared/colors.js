import ThemeManager from 'react-native-color-theme';

export default new ThemeManager({
  bodegaTheme: {
    backgroundColor: '#fff',
    redColor: '#fda497',
    blueColor: '#eaf6fd',
    greenColor: '#c9fdaf'
  },
});